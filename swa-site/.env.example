# No VITE_* environment variables needed for frontend
# All configuration is fetched from the API after authentication

# For local SWA CLI auth simulation (optional):
# GITHUB_CLIENT_ID=your_client_id
# GITHUB_CLIENT_SECRET=your_client_secret
